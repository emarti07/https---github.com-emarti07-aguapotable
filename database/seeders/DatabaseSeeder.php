<?php
namespace Database\Seeders;
use Illuminate\Database\Seeder;
class DatabaseSeeder extends Seeder {
    public function run(){
        // Este seeder solo da instrucciones. Para datos completos, importe sql/aguapotable_mysql_dump.sql en tu MySQL.
        // Opcional: ejecutar DB::unprepared(file_get_contents(database_path('dumps/aguapotable_mysql_dump.sql')));
    }
}
